---
title: Card
description: A content card with optional icon and style variables.
---

import { CardGrid } from '@astrojs/starlight/components'
import Card from 'starlight-plugin-icons/components/Card.astro'
import Aside from 'starlight-plugin-icons/components/Aside.astro'

Example source

```mdx title="page.mdx" "bordered"
import Card from 'starlight-plugin-icons/components/Card.astro'

<Card title="Card" icon="i-ph:thumbs-up-duotone" bordered>
  Card description
</Card>
```

Rendered

<CardGrid>
  <Card title="Card" icon="i-ph:thumbs-up-duotone">
    Simple card
  </Card>
  <Card title="Card" icon="i-ph:thumbs-up-duotone" bordered>
    Card with icon border
  </Card>
</CardGrid>

<Aside type="tip" icon="i-ph:lightbulb-duotone">
  Use the `bordered` prop to add back the colored icon border.
</Aside>

## Customization

### Props

- `title` string — card title
- `icon?` string — icon class
- `bordered?` boolean — show icon in a colored square
- `class?` string — additional CSS classes

### Classes

- `starlight-card`
- `starlight-card--bordered`
- `starlight-card__title`
- `starlight-card__icon`
- `starlight-card__icon-wrapper`
- `starlight-card__content`

### CSS variables

```css title="src/styles/custom.css"
:root {
  --spi-card-icon-size: 1em;
  --spi-card-icon-wrapper-pad: 0.3em;
}
```

### Example

```css title="src/styles/custom.css"
:root {
  --spi-card-icon-size: 1.5em;
  --spi-card-icon-wrapper-pad: 0.5em;
}
```

Rendered

<div class="demo-card">
  <Card title="Bigger Icon" icon="i-ph:thumbs-up-duotone" bordered>
    Tweaked icon and border padding
  </Card>
</div>

<style>{`
  .demo-card {
    --spi-card-icon-size: 1.5em;
    --spi-card-icon-wrapper-pad: 0.5em;
  }
`}</style>
